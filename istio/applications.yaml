apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: istio-components
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - name: istio-base
        path: istio/apps/base.yaml
      - name: istio-gateway
        path: istio/apps/gateway.yaml
  template:
    metadata:
      name: '{{name}}'
      namespace: argocd
    spec:
      project: default
      source:
        repoURL: https://github.com/joferson/-monitoring-stack.git
        targetRevision: HEAD
        path: .
      destination:
        server: https://kubernetes.default.svc
        namespace: istio-system
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true 